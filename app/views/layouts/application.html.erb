<!DOCTYPE html>
<html>
<head>
  <title>IndieThinkTank</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="container">
    <header>
      <div class="header">
        <%= render 'layouts/navbar' %>
      </div>
    </header>

      <div class="alert alert_danger" role="alert">
        <% flash.each do |type, message| %>
          <span class="flash <%= type.to_s %> alert alert_danger"><%= message %></span>
        <% end %>
      </div>

    <%= yield %>
    <br>
    <br>
    <br>
    <footer>
      <div class="footer">
        <%= render 'layouts/navbar' %>
      </div>
    </footer>
    <br>
    <br>
  </div>
</body>
</html>
